# Algo Sync Workspace

This VS Code extension is the local half of the Algo Sync Workspace project. It activates only when the opened workspace contains `.algo-sync.json` and communicates with the companion Chrome/Edge extension on `127.0.0.1`.

See the repository root README for installation and usage instructions.
